.env
.claude/
__pycache__/
*.pyc
.venv/
venv/

# Runtime log + transient local error report (local-only signals)
tracker.log
tracker.log.*
errors.json

# Local-only session handoff notes
handoff.md

# Filter-automation work in progress — not yet ready to push.
update_filter.py
filter_config.json
*.filter
*.filter.bak
